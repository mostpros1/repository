// Change from CommonJS to ESM
// Import babel-jest as an ES module
import { createTransformer } from 'babel-jest';

// Export the transformer function as an ES module
export default createTransformer({
    rootMode: "upward",
});